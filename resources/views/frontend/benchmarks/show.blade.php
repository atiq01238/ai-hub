@extends('frontend.layouts.app')
@section('title',$title.' | AI Orbit')
@section('meta_description',$description)
@section('og_type', 'website')
@push('head')
<script type="application/ld+json">{!! json_encode(['@context'=>'https://schema.org','@type'=>'Dataset','name'=>$benchmark->name.' benchmark results','description'=>$description,'url'=>route('benchmarks.show',$benchmark),'isAccessibleForFree'=>true,'measurementTechnique'=>$benchmark->name,'version'=>$benchmark->version,'sameAs'=>$benchmark->official_url], JSON_UNESCAPED_SLASHES) !!}</script>
<script type="application/ld+json">{!! json_encode(['@context'=>'https://schema.org','@type'=>'BreadcrumbList','itemListElement'=>[['@type'=>'ListItem','position'=>1,'name'=>'Home','item'=>route('home')],['@type'=>'ListItem','position'=>2,'name'=>'Benchmarks','item'=>route('benchmarks.index')],['@type'=>'ListItem','position'=>3,'name'=>$benchmark->name,'item'=>route('benchmarks.show',$benchmark)]]], JSON_UNESCAPED_SLASHES) !!}</script>
@endpush
@section('content')
<section class="section"><div class="container"><div class="section-heading"><span class="eyebrow">VERIFIED AI BENCHMARK</span><h1>{{ $benchmark->name }} Leaderboard</h1><p>{{ $benchmark->description ?: $description }}</p></div>
<div class="card" style="padding:18px;margin-bottom:18px"><strong>{{ $benchmark->category }}</strong> · {{ $benchmark->unit ?: 'score' }} · {{ $benchmark->higher_is_better ? 'Higher is better' : 'Lower is better' }} @if($benchmark->version) · Version {{ $benchmark->version }} @endif @if($benchmark->variant) · {{ $benchmark->variant }} @endif<br><small>Only verified results are included in this public leaderboard.</small> @if($benchmark->methodology_url || $benchmark->official_url)<p><a rel="nofollow noopener" target="_blank" href="{{ $benchmark->methodology_url ?: $benchmark->official_url }}">Official methodology / benchmark source</a></p>@endif</div>
<div class="card" style="overflow:auto"><table class="table"><thead><tr><th>#</th><th>Model / Tool</th><th>Score</th><th>Tested</th><th>Source</th></tr></thead><tbody>@forelse($results as $result)<tr><td>{{ $loop->iteration }}</td><td><strong>{{ $result->benchmarkable?->name ?? 'Unavailable' }}</strong></td><td>{{ number_format((float)$result->score,2) }} {{ $benchmark->unit }}</td><td>{{ $result->tested_at?->format('M j, Y') ?? '—' }}</td><td>@if($result->source_url)<a rel="nofollow noopener" target="_blank" href="{{ $result->source_url }}">{{ $result->source_name ?: 'Source' }}</a>@else{{ $result->source_name ?: '—' }}@endif</td></tr>@empty<tr><td colspan="5">No verified results published yet.</td></tr>@endforelse</tbody></table></div>
<section style="margin-top:28px"><h2>About {{ $benchmark->name }}</h2><p>This leaderboard uses the latest verified result available for each listed entity. Results retain their source and test date so readers can evaluate freshness and methodology.</p></section></div></section>
@endsection
